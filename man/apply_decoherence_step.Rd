% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{apply_decoherence_step}
\alias{apply_decoherence_step}
\title{Apply one decoherence step.}
\usage{
apply_decoherence_step(state, channel, p)
}
\arguments{
\item{state}{represents the state matrix of the walk.}

\item{channel}{represents the selected decoherence channel.}

\item{p}{represents the decoherence strength.}
}
\value{
the updated normalized state matrix after one decoherence step.
}
\description{
This applies one sampled decoherence step to the current walk
state and then normalizes the result. After sampling outcome \eqn{k}, the updated state is
\deqn{
|\psi\rangle \leftarrow
\frac{\widetilde{K}_k |\phi\rangle}{\sqrt{p_k}},
}
where \eqn{p_k = \|\widetilde{K}_k |\phi\rangle\|^2}.
}
\examples{
set.seed(1)
state <- initialize_state(1, "symmetric")
apply_decoherence_step(state, "dephasing", 0.1)

}
