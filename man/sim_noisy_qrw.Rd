% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{sim_noisy_qrw}
\alias{sim_noisy_qrw}
\title{Simulate a noisy Hadamard quantum random walk.}
\usage{
sim_noisy_qrw(T, N, channel, init_coin, p, seed = NULL)
}
\arguments{
\item{T}{represents the number of walk steps.}

\item{N}{represents the number of Monte Carlo trajectories.}

\item{channel}{represents the selected decoherence channel. The allowed
choices are \code{"dephasing"} and \code{"depolarizing"}.}

\item{init_coin}{represents the initial coin state. The allowed choices are
\code{"right"}, \code{"left"}, and \code{"symmetric"}.}

\item{p}{represents the decoherence strength.}

\item{seed}{an optional random seed used for reproducibility.}
}
\value{
a list containing the number of steps, the time indexed position
distributions, the mean positions, and the variances.
}
\description{
This simulates a decohering Hadamard quantum random walk on a
line using Monte Carlo trajectories. The Monte Carlo estimate of the position distribution is
\deqn{
\widehat{P}_t(x) = \frac{1}{N}\sum_{j=1}^N P_t^{(j)}(x),
}
where \eqn{N} is the number of trajectories.
}
\examples{
set.seed(1)
sim_noisy_qrw(T = 3, N = 5, channel = "dephasing",
              init_coin = "symmetric", p = 0.1, seed = 1)
              
}
